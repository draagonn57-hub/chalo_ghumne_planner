# Test credentials

## Admin (seeded automatically at backend startup via `backend/seed.py`)
- **Email**: `admin@tourplanner.com`
- **Password**: `Admin@12345`
- **Role**: `admin` — has access to `/admin` panel

## Regular users
Create via `/signup` (min 6-char password). No pre-seeded regular user.
